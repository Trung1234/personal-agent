<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.personal_agent.repository.ScheduleMapper">

    <insert id="insert" parameterType="com.example.personal_agent.model.Schedule">
        INSERT INTO Schedules (task_id, subtask_id, title, start_time, end_time, google_event_id, location)
        VALUES (#{taskId}, #{subtaskId}, #{title}, #{startTime}, #{endTime}, #{googleEventId}, #{location})
    </insert>

</mapper>